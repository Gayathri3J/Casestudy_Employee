<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Employee</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/header') %>

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <h2 class="mb-4 text-center">Edit Employee</h2>
        <form action="/basic/edit/<%= employee.id %>" method="POST" class="p-4 border shadow-sm rounded bg-light">
          <div class="mb-3">
            <label>Name</label>
            <input type="text" name="name" value="<%= employee.name %>" required class="form-control">
          </div>
          <div class="mb-3">
            <label>Designation</label>
            <input type="text" name="designation" value="<%= employee.designation %>" required class="form-control">
          </div>
          <div class="mb-3">
            <label>Location</label>
            <input type="text" name="location" value="<%= employee.location %>" required class="form-control">
          </div>
          <div class="mb-3">
            <label>Salary</label>
            <input type="number" name="salary" value="<%= employee.salary %>" required class="form-control">
          </div>
          <div class="mb-3">
            <label for="image" class="form-label">Employee Image URL</label>
            <input type="url" name="image" class="form-control" value="<%= employee.image %>" required>
          </div>
          <div class="d-flex justify-content-center">
            <button type="submit" class="btn btn-outline-primary rounded-pill px-5">Update</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
